$$Code tested, it won't work.
$$We don't control in the symbol table if we have more than two if's, the idname can be ambiguous
int a;
int b;
b = 0;
a = 0;
if (a == 0) {
    a = b + 1;
}
if (a != 1) {
    b = a + 1;
}